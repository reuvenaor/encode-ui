---
description: The registry taxonomy — every group with its item count
---

Call the `list_groups` tool on the encode-ui MCP server (no arguments).
Present the groups as a compact table: slug — label — count, plus the total.
Suggest `/encode-ui:list-components <group>` to open one.
