---
description: Full membership of one registry group — the enumeration a search cannot be
argument-hint: <group-slug>
---

Call the `list_components` tool on the encode-ui MCP server with
`group: $ARGUMENTS`. Present every item as name — description. If the slug is
unknown, call `list_groups` and offer the valid slugs.
