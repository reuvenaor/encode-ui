---
description: The full TSX source of a component (append " demo" for its usage example)
argument-hint: <component-name> [demo]
---

Call the `get_component_source` tool on the encode-ui MCP server with the
component name from $ARGUMENTS; if $ARGUMENTS ends with the word `demo`, pass
`part: "demo"` as well. Show the returned code in a fenced block. Gated
components answer with a sign-in remedy — relay it as-is.
