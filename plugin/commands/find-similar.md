---
description: Components related to one you name — same group, shared composition
argument-hint: <component-name>
---

Call the `find_similar` tool on the encode-ui MCP server with
`name: $ARGUMENTS`. Present the related components as name — group —
description, with install commands. If the name does not resolve, run
`search_components` with it as a query first and offer the closest matches.
