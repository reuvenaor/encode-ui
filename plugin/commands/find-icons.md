---
description: Verified lucide icon names — search by concept, or check exact spellings
argument-hint: <query>
---

Call the `find_icons` tool on the encode-ui MCP server with
`query: $ARGUMENTS`. Present the verified icon names (legacy aliases resolve —
Home → House) with the named-import form:
`import { House } from 'lucide-react'`. Never guess an icon name that the tool
did not return.
